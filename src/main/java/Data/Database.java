package Data;

import java.util.HashMap;

public class Database {
    public static HashMap<String, User> userTable = new HashMap<>();
    public static HashMap<Integer, Book> bookTable = new HashMap<>();
    public static HashMap<Integer, Movie> movieTable = new HashMap<>();
    public static HashMap<Integer, ProductStatue> productStatueTable = new HashMap<>();
}
